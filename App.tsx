// Entry point used by expo/AppEntry.js when Expo Go loads the app.
// Re-exports the expo-router root component so routing via app/ works.
export { App as default } from "expo-router/build/qualified-entry";
